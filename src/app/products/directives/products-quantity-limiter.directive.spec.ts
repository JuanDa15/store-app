import { ProductsQuantityLimiterDirective } from './products-quantity-limiter.directive';

describe('ProductsQuantityLimiterDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductsQuantityLimiterDirective();
    expect(directive).toBeTruthy();
  });
});
